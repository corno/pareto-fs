{
  "name": "pareto-fs",
  "version": "0.3.1",
  "description": "fs functionality for pareto projects",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "lint": "tslint --project tsconfig.json ./src/*",
    "pubMin": "rd /s /q .\\dist && git diff --exit-code && npm run u2l && tsc && npm run lint && npm run test && npm version minor && git push && npm publish",
    "pubPatch": "rd /s /q .\\dist && git diff --exit-code && npm run u2l && tsc && npm run lint && npm run test && npm version patch && git push && npm publish",
    "test": "tsc && node ./dist/test",
    "u2l": "ncu -u --packageFile package.json && npm install"
  },
  "keywords": [],
  "author": "corno",
  "license": "ISC",
  "types": "dist/index.d.ts",
  "prepublish": "tsc",
  "repository": {
    "type": "git",
    "url": "https://github.com/corno/pareto-fs.git"
  },
  "files": [
    "dist/*"
  ],
  "dependencies": {
    "@types/node": "^12.12.5",
    "pareto": "^0.23.1"
  },
  "devDependencies": {
    "steroid-template-utils": "^0.2.0"
  }
}
